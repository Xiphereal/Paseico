<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.paseico">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".SelectedPoiActivity"
            android:label="@string/title_activity_selected_poi"></activity>

        <activity android:name=".OrganizationCouponsActivity" />
        <activity android:name=".CreateCouponActivity" />
        <activity
            android:name=".MainMenuActivity"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".mainMenu.userCreatedRoutes.IntroduceNewRouteDataActivity" />
        <activity android:name=".mainMenu.userCreatedRoutes.CreateNewRouteActivity" />
        <activity android:name=".RouteRunnerNotOrderedActivity" />
        <activity android:name=".RouteRunnerOrderedActivity" />
        <activity android:name=".TemporalRoutesMenu" />
        <activity android:name=".RouteFinishedActivity" />
        <activity android:name=".mainMenu.searchUsers.FollowersActivity" />
        <activity android:name=".RouteInformationActivity" />
        <activity android:name=".login.RegisterActivity" >

        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".login.LogInActivity" />
        <activity android:name=".login.RegisterOrganizationActivity" />
        <activity android:name=".MainMenuOrganizationActivity" />
        <activity android:name=".ModifyRouteActivity" />
        <activity android:name=".RouteInfModifyActivity" />
        <activity android:name=".login.OpeningActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>
    <meta-data
        android:name="com.google.android.gms.version"
        android:value="@integer/google_play_services_version" />
    <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="AIzaSyAMLXmyf_ai85V6PdlA7jYhjL-nSgHl7mA" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

</manifest>